#include <bits/stdc++.h>
using namespace std;
#define pb push_back
#define ll long long

const int N = 1e5 + 10;
vector<int> g[N];
int level[N];
void bfs(int source)
{
    deque<int> cx;
    cx.push_front(source);
}
int main()
{
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    return 0;
}